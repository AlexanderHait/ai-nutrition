TeddY UI/UX v20.5 — CSS BUILD FIX

Применять ПОСЛЕ v20.1 → v20.2 → v20.3 → v20.4.

Исправлено:
• удалены буквальные последовательности \n, случайно попавшие в app/globals.css;
• @media снова являются корректными CSS at-rules;
• проверен баланс фигурных скобок;
• CSS проверен парсером tinycss2 — ошибок синтаксиса нет.

Изменён только app/globals.css.
