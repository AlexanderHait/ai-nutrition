import {getSupabaseAdmin} from '@/lib/supabase-admin';import {fmt} from '@/lib/data';export const dynamic='force-dynamic';export default async function Page(){const s=getSupabaseAdmin();const{data,error}=await s.from('subscriptions').select('*').order('created_at',{ascending:false});return <><div className="pageHead"><div><p>AI‑Nutrition / Админка</p><h1>Подписки</h1><span>Платёжный провайдер подключим позже. Структура уже готова.</span></div></div><section className="card">{error?<div className="notice">Сначала выполни миграцию 002_portal.sql в Supabase.</div>:(data||[]).length?(data||[]).map((x:any)=><div className="row" key={x.id}><div><b>{x.plan}</b><small>Telegram ID {x.chat_id}</small></div><div><b>{x.status}</b><small>{fmt(x.price_rub)} ₽</small></div></div>):<p className="muted">Подписок пока нет.</p>}</section></>}
