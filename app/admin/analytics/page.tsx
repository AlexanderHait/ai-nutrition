import {allData,fmt} from '@/lib/data';export const dynamic='force-dynamic';export default async function Page(){const{profiles,meals,logs}=await allData();const days=Array.from({length:14},(_,i)=>{const d=new Date();d.setDate(d.getDate()-13+i);return new Intl.DateTimeFormat('sv-SE',{timeZone:'Europe/Moscow'}).format(d)});return <><div className="pageHead"><div><p>AI‑Nutrition / Админка</p><h1>Аналитика</h1><span>Последние 14 дней</span></div></div><div className="stats"><Stat l="Всего клиентов" v={fmt(profiles.length)}/><Stat l="Всего приёмов" v={fmt(meals.length)}/><Stat l="Сообщений (выборка)" v={fmt(logs.length)}/></div><section className="card top"><h2>Активность питания</h2><div className="bars">{days.map(d=>{const n=meals.filter(m=>m.eaten_day===d).length;return <div className="barCol" key={d}><div className="bar" style={{height:`${Math.max(6,Math.min(180,n*18))}px`}} title={`${n} приёмов`}></div><small>{d.slice(5)}</small><b>{n}</b></div>})}</div></section></>};function Stat({l,v}:{l:string;v:string}){return <div className="stat"><span>{l}</span><b>{v}</b></div>}
