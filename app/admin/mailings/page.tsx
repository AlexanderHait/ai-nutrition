import {Send, Clock3, Users} from 'lucide-react';import {getSupabaseAdmin} from '@/lib/supabase-admin';
export const dynamic='force-dynamic';
export default async function Page(){const s=getSupabaseAdmin();const {data,error}=await s.from('mailings').select('*').order('created_at',{ascending:false}).limit(30);return <><div className="pageHead"><div><p>AI‑Nutrition / Админка</p><h1>Рассылки</h1><span>Сообщения клиентам прямо в Telegram</span></div></div>{error&&<div className="notice">Для рассылок выполни миграцию <b>004_mailings.sql</b> в Supabase.</div>}<div className="mailingLayout top"><section className="card"><div className="sectionTitleRow"><div><h2>Новая рассылка</h2><span className="muted">Отправить сейчас или запланировать</span></div><Send size={20}/></div><form className="mailingForm" action="/api/admin/mailings" method="post"><label>Название<input name="title" placeholder="Например: Напоминание о фото" required/></label><label>Получатели<select name="segment"><option value="all">Все клиенты</option><option value="basic">Только Basic</option><option value="premium">Только Premium</option></select></label><label>Сообщение<textarea name="content" rows={7} placeholder="Текст сообщения для рассылки…" required/></label><label>Дата и время<input type="datetime-local" name="scheduled_at"/></label><div className="mailActions"><button className="primary" name="action" value="send" type="submit">Отправить сейчас</button><button name="action" value="schedule" type="submit">Запланировать</button></div></form></section><section className="card"><div className="sectionTitleRow"><div><h2>История</h2><span className="muted">Последние рассылки</span></div></div><div className="mailHistory">{(data||[]).length?(data||[]).map((x:any)=><div className="mailRow" key={x.id}><i>{x.status==='sent'?<Send/>:<Clock3/>}</i><div><b>{x.title}</b><span>{x.segment==='all'?'Все клиенты':x.segment.toUpperCase()} · {x.status==='sent'?'Отправлено':'Запланировано'}</span></div><strong><Users size={13}/>{x.status==='sent'?x.sent_count:x.recipient_count}</strong></div>):<p className="muted">Рассылок пока нет.</p>}</div></section></div></>}
