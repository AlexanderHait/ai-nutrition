import {requireClient} from '@/lib/auth';import {clientData,dayKey,fmt,sumMeals} from '@/lib/data';export const dynamic='force-dynamic';export default async function Page(){const s=await requireClient(),d=await clientData(s.chatId!);const tm=d.meals.filter(m=>m.eaten_day===dayKey()),sum=sumMeals(tm),goal=Number(d.settings?.kcal_target||2000);return <><div className="pageHead"><div><p>Сегодня</p><h1>{d.profile?.first_name?`Привет, ${d.profile.first_name}`:'Твой рацион'}</h1><span>Данные автоматически синхронизируются с Telegram‑ботом</span></div></div><div className="heroKcal"><div><span>Калории</span><b>{fmt(sum.kcal)}</b><small>из {fmt(goal)} ккал</small></div><div className="progress"><i style={{width:`${Math.min(100,sum.kcal/goal*100)}%`}}/></div></div><div className="stats"><Stat l="Белки" v={`${fmt(sum.prot,1)} г`}/><Stat l="Жиры" v={`${fmt(sum.fat,1)} г`}/><Stat l="Углеводы" v={`${fmt(sum.carb,1)} г`}/><Stat l="Приёмов" v={String(tm.length)}/></div><section className="card top"><h2>Сегодня</h2>{tm.map(m=><div className="meal" key={m.id}><div><b>{m.dish}</b><small>{fmt(m.grams)} г · {new Date(m.eaten_at).toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit',timeZone:'Europe/Moscow'})}</small></div><div><b>{fmt(m.kcal)} ккал</b><small>Б {fmt(m.prot,1)} · Ж {fmt(m.fat,1)} · У {fmt(m.carb,1)}</small></div></div>)}{!tm.length&&<p className="muted">Сегодня ещё нет приёмов пищи.</p>}</section></>};function Stat({l,v}:{l:string;v:string}){return <div className="stat"><span>{l}</span><b>{v}</b></div>}
