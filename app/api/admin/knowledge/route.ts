import {NextResponse} from "next/server";import {requireAdmin} from "@/lib/auth";import {getSupabaseAdmin} from "@/lib/supabase-admin";
export async function POST(req:Request){await requireAdmin();const f=await req.formData(),id=Number(f.get("id")),action=String(f.get("action")||"");const db=getSupabaseAdmin();if(action==="approve")await db.from("coach_knowledge").update({approved_for_ai:true,evidence_level:"reviewed"}).eq("id",id);if(action==="disable")await db.from("coach_knowledge").update({approved_for_ai:false}).eq("id",id);return NextResponse.redirect(new URL("/admin/knowledge",req.url));}