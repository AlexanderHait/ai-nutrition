# Передача состояния — 04.08.2026

Что сделано в последней сессии, что проверено и с чего продолжать.
Общий контекст проекта — в `CLAUDE.md`, он читается автоматически.

---

## Коротко

Оплата запущена и работает на реальных деньгах. Введён бесплатный уровень.
Починена цепочка регистрации: создание аккаунта → подтверждение почты → вход →
первичная анкета. Всё это было сломано и чинилось по ходу.

**Главная незакрытая проблема: с 1 августа не сохраняется ни один приём пищи.**

---

## Оплата ЮKassa — работает

* Магазин `1424467`, ключ и переменные заведены в Vercel владельцем
* Чеки 54-ФЗ **выключены** (владелец самозанятый; ЮKassa прекратила автоотправку
  чеков для самозанятых 29.12.2025, чеки идут через «Мой налог»)
* Вебхук настроен на `payment.succeeded` и `payment.canceled`
* Проверено живой оплатой Premium 2990 ₽ — подписка активировалась

Была ошибка «Не удалось подготовить заказ»: функция
`prepare_yookassa_order_account_v1` падала на неоднозначных именах колонок
(42702). Исправлено миграцией, защиты от подложной суммы и чужого аккаунта
проверены.

**Осталось владельцу:** загрузить логотип магазина в ЛК ЮKassa — на странице
оплаты он берётся оттуда, из кода не задаётся.

---

## Уровни подписки

| Уровень | Фото-анализы | AI-запросы | Коуч |
| --- | --- | --- | --- |
| free | 3 / мес | 5 / мес | нет |
| basic | 40 / мес | 20 / мес | нет |
| premium | без лимита | без лимита | весь |

Раньше каждый новый пользователь получал бессрочный активный `basic` бесплатно —
продавать было нечего. Теперь новый профиль заводится как `free`.

Существующие 7 пользователей не затронуты: их строки помечены `legacy_basic`,
проверено поимённо. Пробные 3 дня Premium сохранены.

Признак «оплачено» — `provider` в `subscription_lifecycle`. Значение
`system_default` означает бесплатный уровень.

---

## Что чинилось по цепочке регистрации

Все четыре ошибки были найдены и закрыты, каждая проверена на базе с откатом:

1. **Регистрация падала.** Триггер профиля писал `plan='free'`, а ограничение
   допускало только `basic`/`premium`. Ограничения расширены.
2. **Письмо вело на `localhost:3000`.** Site URL в Supabase Auth остался с
   разработки. Владелец исправил в настройках, код был верным.
3. **Вход не срабатывал молча.** В браузере оставалась cookie администратора,
   `session()` читает её первой и возвращала роль admin, `requireClient`
   выбрасывал обратно на форму. Клиентский вход теперь гасит эту cookie.
4. **Анкета не сохранялась.** Две причины: у `client_settings` не было
   уникальности по `account_id` (upsert падал), и триггеры AI Core падали с
   `invalid_chat_id` у пользователей без Telegram.

---

## 🔴 Первое, чем заняться: дневник не работает с 1 августа

| Дата | Сохранено приёмов |
| --- | --- |
| 26–31 июля | 5–41 в день |
| 1–4 августа | **0** |

Черновики при этом создаются: за 2–3 августа 5 штук, **все со статусом
`cancelled`**, ни одного `saved`. Бот распознаёт еду и показывает карточку, но до
таблицы `meals` она не доходит.

Совпадает по времени с переездом n8n на Railway и заменой Telegram-триггера
01.08. Копать в сабворкфлоу `2z8MgeEZ8kt4MeBg` (callbacks) и функцию
`commit_meal_draft_v20`.

Смежное: 02.08 было 3 ошибки ingress на нажатие «🗑 Удалить» —
`Telegram edit failed: Bad Request`. Редактируется сообщение, которого уже нет.
Нужно обернуть Telegram edit так, чтобы неудача не роняла выполнение.

---

## Остальные открытые пункты по приоритету

2. **Клиент без Telegram не может вести дневник.** В `meals` поле `chat_id`
   обязательное. Человек регистрируется по почте, платит, заполняет анкету — и не
   может записать ни одного приёма. Нужно разрешить `NULL` и научить дневник
   работать по `account_id`. Задевает весь конвейер питания.
3. **Мёртвый эндпоинт `/api/bot/meal`.** Пишет в несуществующие поля
   (`meal_name`, `calories`, `user_id`, `telegram_user_id`) вместо реальных
   (`dish`, `kcal`, `chat_id`). Падал бы при каждом вызове. Переписать или удалить.
4. **RLS без политик на 52 таблицах.** Вся защита держится на ключе
   `service_role`. Решить осознанно: писать политики или зафиксировать текущую
   модель и защищать ключ.
5. **Leaked Password Protection выключен** в Supabase Auth. Вход по паролю есть,
   включается тумблером.
6. **Нет `package-lock.json`.** Сборка на Vercel может подтянуть другие версии
   библиотек — сайт способен сломаться без правок кода.
7. **Ноль автотестов** на 131 файл и 48 API-маршрутов. Начать стоит с оплаты и
   авторизации — там уже ловили поломки постфактум.
8. **Дашборды всё ещё текстовые.** Просьба владельца «цифры в графики» выполнена
   только на странице подписки. Главная, прогресс и карточка клиента ждут.
9. **Изображения без оптимизации** — в `next.config.ts` нет блока `images`.
10. **Две функции без `search_path`** — `coach_score_symmetric_v1`,
    `coach_score_protein_v1`. Чинится одной строкой.

---

## Как работать с владельцем

Не программист. Нужен результат, а не разбор кода.

* Отвечать по-русски, коротко, **с заголовками и таблицами** — сплошной текст не
  читается
* Не пересказывать процесс («смотрю», «проверяю») — только итог
* Мелкий шрифт в интерфейсе не годится: минимум 10.5px, это уже поправлено
  глобально
* Спрашивать, когда решение меняет продукт или деньги; остальное решать самому
* Работать до готового результата и выдавать **одну финальную версию**, а не
  череду промежуточных

---

## Технические договорённости

* Правки идут в ветку, затем сливаются в `main` — Vercel собирает production
  из `main`
* Каждое изменение проверяется `npx next build` перед выкладкой
* Изменения базы — только через `apply_migration`, файл миграции кладётся в
  `supabase/migrations/`
* Разрешения на инструменты — в `.claude/settings.json`. Supabase уже разрешён.
  Расширять этот файл владелец должен сам: агенту запрещено выдавать себе права
* Секреты в чат не присылать. Ключ ЮKassa один раз попал в переписку и был
  отозван
